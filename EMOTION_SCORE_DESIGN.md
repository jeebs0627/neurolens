# ‘오늘의 내 감정지수’ 산출 설계안 (v1)

## 점수가 뜻하는 것

현재 시점에 본인이 보고한 **감정의 쾌·불쾌 정도**를 0–100으로 환산한다. 0은 매우 불쾌, 50은 중립, 100은 매우 유쾌다. 100이 ‘정신건강이 좋음’을 뜻하거나 0이 질환을 뜻하지 않는다. 이 값은 우울 위험도, 정서회복력, 행복도, 감정의 강도와도 다르다. 결과에는 ‘뉴로렌즈 자체 평가결과로 의료적 진단을 대체하지 않습니다.’를 함께 표시한다.

기존 사전 체크인은 감정 이름을 복수 선택하게 하지만 강도와 종합적인 쾌·불쾌를 측정하지 않았다. 이번 적용에서 아래 1–9 문항을 추가했다. 이전 기록처럼 이 문항이 없는 체크인에는 임의의 100점 점수를 계산하지 않고 ‘—’로 표시한다.

현재 실제 검사 경로는 `index.html` → `/checkin`(`indexadmin.html`) → `resultadmin.html`이다. 요청한 ‘오늘 내 감정 상태’ 카드도 `resultadmin.html`에 있다. `result.html`은 별도 샘플 리포트다. 체크인 폼과 `care-preview.js`의 `normalizeCheckin()`에 새 응답을 보존하고, `resultadmin-care.js`에서 계산해 카드 오른쪽에 표시한다.

저장소의 `indexadmin.html`에는 가상 샘플용 `gazeMetrics`와 `depressionScreening`이 있으나, 실제 측정 엔진의 원본 결과 예시는 없다. 콜백은 받은 JSON을 그대로 전달한다. 따라서 실측 응답마다 시선 품질·집중·탐색·정서 선별 구간이 들어오는지 현재 코드만으로 검증할 수 없다.

## 측정 매트릭스

| 확보된 항목 | 현재 형식 | 점수에 반영 | 역할과 이유 |
| --- | --- | ---: | --- |
| 지금의 기분 | 15개 감정 이름, 복수 선택 | 0% | 감정 종류와 복합 감정을 설명한다. 강도와 선택 순서가 없어 숫자 가중치를 정할 근거가 없다. |
| 컨디션 | 1–5 | 0% | 활력·피로의 단서다. 낮은 활력은 편안한 상태에서도 가능하므로 쾌·불쾌 점수에 더하지 않는다. |
| 기대·걱정 | 각각 있음/없음/모름 | 0% | 사건에 관한 생각을 설명한다. 걱정이 있어도 현재 감정이 좋을 수 있다. |
| 마음에 걸리는 일 | 8개 주제 중 하나 | 0% | 케어 제안의 맥락이다. 특정 주제를 감점하지 않는다. |
| Big5 백분위 | O·C·E·A·N | 0% | 평소 성향이다. 오늘의 감정 상태를 직접 측정하지 않는다. |
| MBTI·RIASEC·직무적합도 | 유형·점수 | 0% | 성향·흥미·진로의 지표이므로 오늘의 감정과 별도로 둔다. |
| 시선 품질·집중·탐색 | 있을 때만 범주형 | 0% | 품질은 측정 신뢰도 설명에만 쓴다. 집중·탐색에서 쾌·불쾌로 변환하는 검증된 모델과 기준값이 없다. |
| 정서 선별 구간 | 있을 때만 낮음/경계/높음 | 0% | 경계·높음은 별도 도움 안내에 쓰며, 감정 점수에서 임의 감점하지 않는다. 숫자만 있는 EARP에는 구간을 추정하지 않는다. |
| **현재 감정 평가** | **1–9, 필수** | **100%** | 지금 경험하는 전반적인 쾌·불쾌를 본인이 직접 보고한다. |

## 계산식과 처리 순서

사전 체크인에 문항 하나를 추가했다.

> **지금 기분은 전반적으로 어떤가요?** 1 매우 불쾌 — 5 보통 — 9 매우 유쾌

`V`를 이 문항의 1–9 정수 응답이라고 하면:

```text
오늘의 내 감정지수 = round(100 × (V − 1) ÷ 8)
```

예: `V=3 → 25점`, `V=5 → 50점`, `V=7 → 75점`. 선형 변환이므로 25점이 75점보다 ‘정신건강이 3배 나쁘다’는 뜻은 없다. 1점 단위 표시는 UI 표기 형식일 뿐 그만큼의 측정 정확도를 입증하지 않는다. 권장 화면은 숫자와 함께 ‘지금 기분: 다소 유쾌함’ 같은 원래 응답을 보여주는 방식이다.

검사 결과 객체와 검사 시각이 있고, 체크인부터 검사 완료까지 순서가 맞으며, 한국 시각 기준 체크인 날짜가 오늘일 때만 `status=ok`와 숫자를 반환한다. 다른 날짜에 다시 열면 `expired_checkin`을 반환해 지난 점수를 ‘오늘’로 재사용하지 않는다. 응답이 없으면 `needs_valence`, 검사가 끝나지 않았으면 `awaiting_measurement`다. 기분 이름·에너지·시선 품질·명시된 선별 구간은 숫자와 별도 메타데이터로 보존한다. 경계·높음 선별 구간은 점수 높낮이와 무관하게 도움 안내를 우선 표시할 수 있도록 플래그를 반환한다.

## 더 정확하게 하려면

이번 적용에서 **현재 감정 1–9** 문항 한 개를 추가했다. 복합 감정을 더 잘 설명하려면 ‘지금 긍정적인 감정의 강도’와 ‘지금 힘든 감정의 강도’를 각각 0–10으로 물을 수 있다. 두 강도는 서로 독립적으로 높을 수 있으므로 한 축에 억지로 합치지 않고, 혼합 상태 설명과 모델 검증에 활용한다. 현재의 컨디션 질문은 그대로 두되 감정 점수와 구분한다. 체크인 시각도 계속 기록한다.

시선값을 수치에 실제로 섞으려면 자극 종류·조도·보정 품질·사용자군을 고정하거나 통제하고, **동일 시점의 독립적인 자기보고 정답값**에 대해 예측 모델을 학습해야 한다. 사람 단위로 나눈 별도 검증 자료에서 자기보고 단독보다 오차가 유의하게 줄어드는지, 연령·성별·측정 기기별 편향이 없는지 확인한 뒤에만 가중치를 정한다. 그전의 시선 가중치는 0이다. 이 0/100 분배는 임상적으로 검증된 최적 계수가 아니라, 현재 근거가 닿는 범위를 지키기 위한 제품 설계 결정이다.

## 근거와 한계

- Russell의 감정 원형 모형은 쾌·불쾌와 각성도를 구분한다. 따라서 현재 컨디션(활력)을 감정의 좋고 나쁨으로 취급하지 않는다. [Russell, 1980](https://pdodds.w3.uvm.edu/research/papers/others/1980/russell1980a.pdf)
- SAM 연구는 쾌·불쾌와 각성도를 짧은 자기보고로 별도 평가하는 방식을 검증했다. 여기서 제안한 한국어 한 문항과 0–100 선형 환산 자체가 별도로 임상 검증되었다는 뜻은 아니다. [Bradley & Lang, 1994](https://pubmed.ncbi.nlm.nih.gov/7962581/)
- PANAS 원 연구는 긍정 정서와 부정 정서가 비교적 독립적인 차원임을 제시한다. 복합 감정의 다중 선택을 평균해 한쪽 감정을 지우지 않는다. [Watson et al., 1988](https://pubmed.ncbi.nlm.nih.gov/3397865/)
- Big5 일상 상태의 개인 내 변동을 확인한 경험표집 연구에 비추어, 평소 성향 백분위를 그날의 감정 점수로 곧바로 치환하지 않는다. [Fleeson, 2001](https://pubmed.ncbi.nlm.nih.gov/11414368/)
- 시선 추적만으로 느끼는 감정의 방향을 안정적으로 판정할 수 있다는 근거는 충분하지 않다. 동공 반응은 밝기의 영향을 받고 긍정·부정 자극 모두에서 커질 수 있다. [Lim et al., 2020](https://www.mdpi.com/1424-8220/20/8/2384), [Bradley et al., 2008](https://onlinelibrary.wiley.com/doi/full/10.1111/j.1469-8986.2008.00654.x)

기존 `care.js`의 ‘오늘의 마음 컨디션 지수’는 Big5·EARP·에너지·이모지·루틴에 가중치를 준 별도 휴리스틱이다. 두 점수를 동일한 척도처럼 비교하거나 이름만 바꾸어 재사용하지 않는다.
